# v1.22.0 Ship Checklist — "Mission Control for the agent swarm" (P0 Swarm View)

Captures the FULL path from "code-complete + pre-ship hardened" to "shipped".
v1.22.0 is **code-complete**; backend S2 (v0.48) + S6 (v0.49) are **APPLIED to
prod**; the feature ships **DARK** (`HelperConfig.swarm_enabled=False`). The
steps in §6 are **gated on the user** (ASC/Play accounts, VM smoke, a physical
Dynamic-Island device) — they cannot be done remotely.

This doc is the artifact a pre-ship hardening sweep produced so the gated
remainder is mechanical. Sweep plan: `~/.claude/plans/inherited-humming-finch.md`.
Companion archive: `PROJECT_FIX_2026-05-17_v1.22_swarm.md`.

---

## 1. Version Matrix (NOT yet bumped — bump is the first gated step, §6.1)

| Component | FROM | TO | File |
|---|---|---|---|
| Apple `MARKETING_VERSION` | 1.21.0 | 1.22.0 | `CLI Pulse Bar/CLI Pulse Bar.xcodeproj/project.pbxproj` (all build-config blocks) |
| Apple `CURRENT_PROJECT_VERSION` | 62 | 64 | same pbxproj (all blocks) |
| Android `versionName` | "1.21.0" | "1.22.0" | `android/app/build.gradle.kts` |
| Android `versionCode` | 30 | 31 | `android/app/build.gradle.kts` |
| Helper `HELPER_VERSION` | 1.18.0 | **bump at .pkg publish** | `helper/system_collector.py:39` |

### ⚠ Known inconsistency — `HELPER_VERSION` not bumped by `sync-versions.sh`
`scripts/sync-versions.sh` rewrites pbxproj `MARKETING_VERSION` and
`build.gradle.kts` `versionName`/`versionCode` only. `helper/system_collector.py`
`HELPER_VERSION` (currently `1.18.0`) is **not** touched and must be bumped
**manually** as part of the helper `.pkg` publish (§6.2). This is intentional
for this train but easy to forget — verified against the script source, not
prose. Do **not** bump it now (ship is gated).

---

## 2. Pre-Ship Build / Test Verification (local, 2026-05-17)

| Target | Command | Result |
|---|---|---|
| Helper ruff | `ruff check .` (helper/) | ✅ All checks passed |
| Helper pytest (incl. 8 new tests) | `pytest -q` (helper/) | ✅ 566 passed, 1 skipped (8 new tests green; 8/8 in isolation) |
| CLIPulseCore | `swift test` | ✅ All tests passed (incl. RemoteSwarm 4-test suite, 0 failures) |
| HelperSwift | `swift test` | ✅ 336 tests, 0 failures |
| macOS app | `xcodebuild build` "CLI Pulse Bar" (macOS) | ✅ BUILD SUCCEEDED |
| iOS app | `xcodebuild build` "CLI Pulse iOS" (iOS Sim) | ✅ BUILD SUCCEEDED |
| watchOS | `xcodebuild build` "CLI Pulse Watch" (watchOS Sim) | ✅ BUILD SUCCEEDED |
| Widgets ext | `xcodebuild build` "CLI Pulse Widgets" (macOS) | ✅ BUILD SUCCEEDED |
| CLIPulseHelper LoginItem | `xcodebuild build` "CLIPulseHelper" (macOS) | ✅ BUILD SUCCEEDED |
| Android | `./gradlew clean testDebugUnitTest assembleDebug` | ✅ BUILD SUCCESSFUL (JDK17 + ~/Library/Android/sdk) |

Notes:
- Build matrix mirrors `swift-ci.yml` full-matrix destinations exactly.
- The Decision-A cross-reference comments (`SwarmTab.swift` / `iOSSwarmTab.swift`)
  are comment-only (lexically compilation-inert) and were added post-matrix;
  re-verified by CI on the hardening commit.
- Local env: helper venv Python 3.13 (CI uses 3.12 — minor version delta, no
  3.13-specific incompatibility observed); ruff 0.15.13; pytest 9.0.3.
- **Hardening commit `e128555` CI — all green** (2026-05-17, push smoke
  matrix): Swift CI ✅ · Helper CI ✅ · Supabase CI ✅ · Lint ✅. Android CI
  path-filtered (no `android/` change) → **not triggered**, so the §3 known
  release-AAB keystore-fail did not run on this commit. This is the factual
  close of the "re-verified by CI on the hardening commit" expectation above.

---

## 3. Known CI Failure (pre-existing, NOT a regression)

**Android `:app:validateSigningRelease` / "Build release APK + AAB".**
`android-ci.yml` runs `./gradlew assembleRelease bundleRelease`; the implicit
`validateSigningRelease` task fails with *"Keystore file cli-pulse-upload.jks
not found"* because `STORE_PASSWORD`/`KEY_PASSWORD` repo secrets are absent on
the smoke runner. Identical on every prior push (b26b196 / be9ec65 / 54cf1f4).
`testDebugUnitTest` + resource compile pass before this step. **Do not "fix".**
The release lane (with secrets set) is the only place this step is meant to pass.

A locally-observed stray `ic_launcher 2.png` in `android/app/build/intermediates`
(macOS/iCloud duplicate artifact) broke the first local Android build; it is in
gitignored generated output only — source res tree is clean, CI unaffected
(starts from clean checkout). Resolved by `rm -rf android/app/build` + rebuild.

---

## 4. Dark-Gate Verification (the v1.22.0 ship-safety invariant)

The whole release is safe to ship because the feature is dark until a
coordinated enable (§6.3). Verified three ways:

1. **Gemini 3.1 Pro deep review #1 (dark-gate seam): GO.** No code path in
   `remote_hook` writes SwarmStore / calls `_swarm_mark` / any swarm RPC when
   `swarm_enabled=False`; `_swarm_heartbeat` early-returns before import/RPC;
   no mid-flight race (per-invocation in-memory config snapshot).
2. **New pytest** `test_swarm_dark_gate_false_never_touches_swarmstore` —
   asserts zero `resolve_worktree` / zero `SwarmStore.record_activity` even on
   the local-approve path that calls `_swarm_mark("running")`.
3. Existing `test_swarm_heartbeat.py::test_disabled_gate_uploads_nothing`
   already covers the S1b heartbeat `False` gate.

Gemini reviews #2 (data contract: rollup ↔ v0.48 RPC ↔ Swift/Kotlin decode)
and #3 (iOS Live Activity lifecycle, `pushType:nil`): both **GO**.

---

## 5. Prod v0.49 Evaluator Validation (live, transactional, zero-leak)

`public._evaluate_swarm_alerts_internal()` is live on a `* * * * *` pg_cron in
prod (`gkjwsxotmwrgqsvfijzs`). Validated empirically via rollback-only `DO`
blocks (FK pair from `devices`; each forced `RAISE`→total rollback):

| Assertion | Expected | Result |
|---|---|---|
| (a) non-numeric `blocked`/`age` (`"x"`,`""`,`true`,`null`) | no 22P02/22023 | ✅ PASS |
| (b) `blocked:2,age:310` | exactly 1 alert, opaque handle only | ✅ PASS |
| (c0) pre-seeded UNRESOLVED alert | unresolved-clause suppresses → 0 new | ✅ PASS |
| (c1) RESOLVED alert @ now-30s | 60s anti-flap holds → 0 new | ✅ PASS |
| (c2) RESOLVED alert @ now-65s | window expired → 1 new | ✅ PASS |
| (d) `blocked:200000,age:9999999` | clamped (100000 / 43200 min), no overflow | ✅ PASS |
| zero-leak post-check | `alerts`/`remote_swarms`/`webhook_jobs` all 0 | ✅ PASS |

The rolled-back `webhook_jobs` row confirms the AFTER-INSERT trigger is
MVCC-safe (table queue, not synchronous `pg_net`) — no external side-effect
survives rollback.

---

## 6. Gated User-Only Steps — EXECUTE IN ORDER

### 6.1 Version bump (do FIRST)
1. `MARKETING_VERSION` 1.21.0→1.22.0 and `CURRENT_PROJECT_VERSION` 62→64 in
   pbxproj (ALL build-config blocks — `scripts/check-versions.sh` verifies
   parity); `versionName`/`versionCode` in `build.gradle.kts`. Prefer
   `scripts/sync-versions.sh`, then re-read its output (known gaps:
   `feedback_sync_versions_script`).
2. Run `scripts/check-versions.sh` → Apple/Android parity green.
3. Commit "chore: bump to v1.22.0 (build 64 / Android vc 31)".

### 6.2 Helper .pkg republish
1. Bump `HELPER_VERSION` in `helper/system_collector.py` (see §1 ⚠).
2. `scripts/build_helper_pkg.sh` (Dev ID App + Installer + `AC_NOTARY_PROFILE`).
3. **VM smoke on a clean Windows-managed-free macOS 15** — mandatory before any
   latest.json promote (`feedback_v080_crash_on_launch_incident`: never skip).
4. Only then publish to `cli-pulse/cli-pulse-helper-releases` latest.json.

### 6.3 Coordinated enable (the dark→live flip)
After §6.2 .pkg is live + VM smoke passed:
1. Ship a helper config with `swarm_enabled=true` (Pairing tab).
2. Verify `remote_swarms` row count > 0 in prod.
3. Verify Mac Swarm tab renders; iOS Swarm tab + Live Activity start on a
   real blocked agent.

### 6.4 macOS App Store
`build-appstore.sh` macOS path (MAS strips embedded Swift helper —
`feedback_asc_release_workflow`), Transporter upload, submit.

### 6.5 iOS App Store
Archive iOS scheme (bumped version), Transporter, submit. **`NSSupports-
LiveActivities` is a new capability** surfacing at ASC — add reviewer notes:
v1.22.0 Live Activity is local-state only (no APNs); APNs push is the
documented v1.22.x follow-up.

### 6.6 Android Play
`./gradlew bundleRelease` with keystore + `STORE_PASSWORD`/`KEY_PASSWORD`;
upload AAB (versionCode 31) to Play Console.

### 6.7 DEVID DMG
`scripts/build_devid_dmg.sh` (Dev ID App + notarize); VM smoke; GitHub Release.

---

## 7. Test-Gap Notes (recorded, intentionally not closed in v1.22.0)

- **`WidgetData` backward-compat decode**: type lives in the Widgets extension
  target, not CLIPulseCore — not cleanly unit-testable from the macOS
  `CLIPulseCoreTests` host. Covered structurally by Widgets BUILD SUCCEEDED;
  Swift synthesizes `decodeIfPresent` for optionals. Add a Widgets test bundle
  in v1.22.1.
- **`SwarmActivityAttributes.ContentState` round-trip**: ActivityKit/iOS-gated,
  unavailable in the macOS test host. Covered by iOS scheme BUILD SUCCEEDED +
  Gemini review #3.
- **Attention-sort extraction (Decision A)**: duplicated comparator in
  `SwarmTab.swift` / `iOSSwarmTab.swift` left intentionally duplicated for
  v1.22.0 (cross-ref comments added); extract to a shared CLIPulseCore pure
  func + unit test in v1.22.1.
