name: Swift CI

# Cost-control v1: feature-branch pushes and draft PRs run a cheap
# smoke gate (CLIPulseCore tests + the macOS app build only). The
# full 5-scheme matrix runs only for pushes to main, non-draft PRs,
# the draft→ready transition, and manual dispatch.
#
# macOS runners cost 10x. Reducing 6 jobs → 2 on the common case
# saves ~67% of macOS-minute burn per feature-branch iteration.

on:
  push:
    paths:
      - 'CLI Pulse Bar/**'
      - '!CLI Pulse Bar/docs/**'
      - '!CLI Pulse Bar/screenshots/**'
      - '!CLI Pulse Bar/scripts/**'
      - '!CLI Pulse Bar/codexbar/**'
      - '!CLI Pulse Bar/**/*.md'
      - 'HelperSwift/**'
      - 'SensorProbe/**'
      - 'scripts/build_signed_app.sh'
      - 'scripts/embed_helper_in_archive.sh'
      - 'scripts/PHASE4D_XCODE_SETUP.md'
      - 'CLI Pulse Bar/scripts/build-appstore.sh'
      - 'scripts/check_helper_no_container_touch.sh'
      - 'scripts/check_helper_version_sync.sh'
      - '.github/workflows/swift-ci.yml'
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - 'CLI Pulse Bar/**'
      - '!CLI Pulse Bar/docs/**'
      - '!CLI Pulse Bar/screenshots/**'
      - '!CLI Pulse Bar/scripts/**'
      - '!CLI Pulse Bar/codexbar/**'
      - '!CLI Pulse Bar/**/*.md'
      - 'HelperSwift/**'
      - 'SensorProbe/**'
      - 'scripts/build_signed_app.sh'
      - 'scripts/embed_helper_in_archive.sh'
      - 'scripts/PHASE4D_XCODE_SETUP.md'
      - 'CLI Pulse Bar/scripts/build-appstore.sh'
      - 'scripts/check_helper_no_container_touch.sh'
      - 'scripts/check_helper_version_sync.sh'
      - '.github/workflows/swift-ci.yml'
  workflow_dispatch:

concurrency:
  # Push and pull_request events get distinct group keys (refs/heads/X
  # vs refs/pull/N/merge) so a same-commit push smoke cannot cancel a
  # PR full matrix and vice versa. Within one event stream, newer runs
  # cancel older ones for the same ref.
  group: swift-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  decide:
    name: Decide matrix (smoke vs full)
    runs-on: ubuntu-latest
    timeout-minutes: 2
    outputs:
      matrix: ${{ steps.set.outputs.matrix }}
      run_full: ${{ steps.set.outputs.run_full }}
    steps:
      - id: set
        env:
          EVENT_NAME: ${{ github.event_name }}
          REF: ${{ github.ref }}
          PR_DRAFT: ${{ github.event.pull_request.draft }}
        run: |
          if [[ "$EVENT_NAME" == "workflow_dispatch" ]]; then
            FULL=true
            REASON="manual workflow_dispatch"
          elif [[ "$EVENT_NAME" == "push" && "$REF" == "refs/heads/main" ]]; then
            FULL=true
            REASON="push to main"
          elif [[ "$EVENT_NAME" == "pull_request" && "$PR_DRAFT" != "true" ]]; then
            FULL=true
            REASON="pull_request not draft"
          else
            FULL=false
            REASON="feature push or draft PR — smoke only"
          fi

          if [[ "$FULL" == "true" ]]; then
            MATRIX='{"include":[{"scheme":"CLI Pulse Bar","destination":"platform=macOS"},{"scheme":"CLI Pulse iOS","destination":"generic/platform=iOS Simulator"},{"scheme":"CLI Pulse Watch","destination":"generic/platform=watchOS Simulator"},{"scheme":"CLI Pulse Widgets","destination":"platform=macOS"},{"scheme":"CLIPulseHelper","destination":"platform=macOS"}]}'
          else
            MATRIX='{"include":[{"scheme":"CLI Pulse Bar","destination":"platform=macOS"}]}'
          fi

          {
            echo "run_full=$FULL"
            echo "matrix=$MATRIX"
          } >> "$GITHUB_OUTPUT"
          echo "Decision: $REASON (run_full=$FULL)"
          echo "Matrix: $MATRIX"

  check-versions:
    name: Version drift gate (v1.21 G6)
    # Runs on every push/PR — purely reads source files, no Apple/Play
    # credentials needed. Catches the case where someone bumps the
    # Apple MARKETING_VERSION without bumping Android versionName (or
    # vice versa). Ubuntu runner since this is just `grep` + `sed`.
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4
      - run: bash scripts/check-versions.sh

  check-migration-numbers:
    name: Migration numbers unique
    # Two migrations sharing a number means nobody can later answer "did v0.70
    # run?" — the answer becomes "which v0.70?". Postgres applies both happily,
    # so the damage only surfaces months later during an incident.
    #
    # Real case (2026-07-28): `main` had migrate_v0.70_device_app_version.sql
    # already applied to production while PR #393 independently added
    # migrate_v0.70_provider_accounts.sql. Neither author could have known;
    # review caught it, nothing in the toolchain would have. Parallel branches
    # are normal here, so "check before you name it" is advice that gets missed
    # — this is the part that cannot be.
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4
      - run: bash scripts/check_migration_numbers.sh

  check-helper-guards:
    name: Helper guards (TCC container-touch + version sync)
    # Pure-grep guards over the helper sources. Both defend against failures
    # that are INVISIBLE in CI and unit tests and only surface on a user's
    # Mac: a container touch re-arms the recurring TCC permission dialog
    # (PROJECT_FIX_2026-07-19), and helper-version drift produces a perpetual
    # unclearable "Update available" nag (1.42.0 post-release audit). The
    # container-touch guard existed since #372 but was wired into NOTHING —
    # it caught three real holes when run by hand and then sat idle.
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4
      - run: bash scripts/check_helper_no_container_touch.sh
      - run: bash scripts/check_helper_version_sync.sh

  test-core:
    name: CLIPulseCore unit tests
    needs: decide
    # v1.10.6: bumped macos-14 → macos-15 for Xcode 16+ / Swift tools 6.0.
    # Sentry-cocoa 9.10+ declares swift-tools-version: 6.0 in its Package.swift,
    # which Xcode 15 (macos-14) can't parse → SPM resolve fails before compile.
    runs-on: macos-15
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4

      - name: Show Xcode
        run: xcodebuild -version

      - name: Swift test
        working-directory: CLI Pulse Bar/CLIPulseCore
        env:
          # DEBUG SupabaseConstants.anonKey lazy init triggers fatalError if
          # missing (v1.10.6 P3-6 self-check path). Any non-empty stub is
          # enough — the tests don't hit the network.
          CLI_PULSE_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY || 'ci-stub-anon-key' }}
        # v1.10.6: dropped --parallel. Under macos-15 / Xcode 16 / Swift 6
        # tooling, parallel test execution exposes pre-existing shared-state
        # races in ProviderLimitMigrationTests (UserDefaults) and similar
        # tests that touch global singletons. Serial is ~50% slower but
        # deterministic, and the suite is small enough (~220 tests) that
        # the total wall time is still under 2 min.
        run: swift test

      - name: Swift test (DEVID_BUILD full suite)
        working-directory: CLI Pulse Bar/CLIPulseCore
        env:
          CLI_PULSE_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY || 'ci-stub-anon-key' }}
        # AppUpdater + UpdateVerifier (and the DEVID SubscriptionManager short-circuit)
        # compile ONLY under DEVID_BUILD, so the default `swift test` above never
        # exercises them — a DEVID-only divergence or compile-rot could ship silently.
        # Run the FULL suite under DEVID_BUILD now that SubscriptionTierResolutionTests is
        # DEVID-aware (was the one pre-existing divergence the old filter dodged). This
        # catches the whole class of DEVID-only regressions, not just the updater path.
        run: swift test -Xswiftc -DDEVID_BUILD

  build-apps:
    name: Build ${{ matrix.scheme }}
    needs: decide
    runs-on: macos-15
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.decide.outputs.matrix) }}

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4

      - name: Show Xcode
        run: xcodebuild -version

      - name: Build ${{ matrix.scheme }}
        working-directory: CLI Pulse Bar
        run: |
          set -o pipefail
          xcodebuild build \
            -project "CLI Pulse Bar.xcodeproj" \
            -scheme "${{ matrix.scheme }}" \
            -destination "${{ matrix.destination }}" \
            -configuration Debug \
            -quiet \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGN_STYLE=Manual \
            DEVELOPMENT_TEAM=""

  test-helper-swift:
    # Phase 4D: Swift port of cli_pulse_helper. The HelperSwift/
    # Package contains its own test suite (UDS framing + auth +
    # session manager + hook adapter + descent + settings
    # installer + streaming). Runs on every push that touches
    # the package or this workflow.
    name: HelperSwift Package tests
    needs: decide
    runs-on: macos-15
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4

      - name: Show Xcode
        run: xcodebuild -version

      - name: Swift test (HelperSwift)
        working-directory: HelperSwift
        run: swift test

  test-sensorprobe:
    # System Monitor S3: native Apple-Silicon sensor reader (clipulse-sensors).
    # Pure decoder/aggregation tests (SMC FourCC + value decode, IOReport
    # energy→W, temp classification). The private IOReport/IOHID symbols resolve
    # at runtime via -undefined dynamic_lookup; the live sensor numbers are
    # device-verified on real Apple Silicon, not in CI.
    name: SensorProbe Package tests
    needs: decide
    runs-on: macos-15
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4

      - name: Show Xcode
        run: xcodebuild -version

      - name: Swift build (release) + test (SensorProbe)
        working-directory: SensorProbe
        run: |
          swift build -c release
          swift test

  build-signed-app:
    # Phase 4D P1.D (Codex review): a green CI build only proves
    # something IF it produces the same artifact users will
    # install. `xcodebuild build CODE_SIGNING_ALLOWED=NO` above
    # leaves the helper unembedded + unsigned, so it doesn't
    # validate the actual shippable .app. This job runs the
    # canonical `scripts/build_signed_app.sh` and verifies that
    # the embedded helper + LaunchAgent plist land at the right
    # paths AND the app's sandbox / app-group entitlements
    # survive the manual re-sign.
    name: Signed-app reproducible build (Phase 4D)
    needs: [decide, test-helper-swift]
    runs-on: macos-15
    timeout-minutes: 30
    if: needs.decide.outputs.run_full == 'true' || github.event.pull_request.head.ref == 'phase4d-swift-helper'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4

      - name: Show Xcode
        run: xcodebuild -version

      - name: Run build_signed_app.sh (Debug, ad-hoc signing)
        run: |
          set -o pipefail
          ./scripts/build_signed_app.sh Debug

      - name: Verify embedded helper at Contents/Helpers/
        run: |
          set -o pipefail
          APP="build_app_output/DerivedData/Build/Products/Debug/CLI Pulse Bar.app"
          test -x "$APP/Contents/Helpers/cli_pulse_helper" \
            || (echo "::error::helper binary missing at $APP/Contents/Helpers/" && exit 1)
          file "$APP/Contents/Helpers/cli_pulse_helper" | grep -q "Mach-O" \
            || (echo "::error::helper is not a Mach-O binary" && exit 1)
          echo "OK: helper Mach-O present + executable"

      - name: Verify embedded LaunchAgent plist
        run: |
          set -o pipefail
          PLIST="build_app_output/DerivedData/Build/Products/Debug/CLI Pulse Bar.app/Contents/Library/LaunchAgents/yyh.CLI-Pulse.helper.agent.plist"
          test -f "$PLIST" \
            || (echo "::error::LaunchAgent plist missing" && exit 1)
          # BundleProgram MUST be present (P1.4 contract) — relative
          # path eliminates runtime mutation.
          plutil -extract BundleProgram raw "$PLIST" \
            || (echo "::error::plist missing BundleProgram key" && exit 1)
          echo "OK: LaunchAgent plist present + BundleProgram set"

      - name: Verify entitlements survived re-sign
        run: |
          set -o pipefail
          APP="build_app_output/DerivedData/Build/Products/Debug/CLI Pulse Bar.app"
          ENT_BLOB="$(codesign -d --entitlements :- "$APP" 2>/dev/null)"
          # App must keep sandbox + app-group (would have been
          # stripped by a naive --force --deep re-sign — Codex P1.C).
          if ! grep -q "com.apple.security.app-sandbox" <<< "$ENT_BLOB"; then
            echo "::error::app sandbox entitlement was stripped" >&2; exit 2
          fi
          if ! grep -q "group.yyh.CLI-Pulse" <<< "$ENT_BLOB"; then
            echo "::error::app-group entitlement was stripped" >&2; exit 2
          fi
          # Helper must NOT have inherited the app's sandbox
          # entitlement (it's an unsandboxed LaunchAgent).
          HELPER_ENT="$(codesign -d --entitlements :- "$APP/Contents/Helpers/cli_pulse_helper" 2>/dev/null)"
          if grep -q "com.apple.security.app-sandbox" <<< "$HELPER_ENT"; then
            echo "::error::helper accidentally has app-sandbox entitlement" >&2; exit 2
          fi
          echo "OK: app sandbox + group preserved; helper unsandboxed"

      - name: Verify helper resolves its own path under launchd-style argv0
        run: |
          # Phase 4D iter11 (Codex P1④): the embedded helper must
          # return its real absolute on-disk path from
          # `_NSGetExecutablePath`, NOT from `CommandLine.arguments
          # .first`. Under launchd, argv[0] is the
          # `ProgramArguments[0]` label string ("cli_pulse_helper"),
          # not the bundle path. Pre-fix, the helper would compute
          # something like `/cli_pulse_helper` and embed it in the
          # `claude --settings` inline JSON, breaking structured
          # approvals.
          #
          # Simulate launchd's argv0 with `exec -a` and confirm
          # the helper resolves its real bundle path. We resolve
          # `HELPER` to an absolute path via `$(pwd)` first so
          # the `cd /` test case can still find the binary
          # (otherwise sh resolves the relative path against
          # the new cwd `/` and dies).
          #
          # Phase 4E e2e fix (2026-05-07): once the helper has
          # restricted entitlements (application-groups), AMFI
          # rejects ad-hoc-signed helpers at exec time with
          # `OS_REASON_EXEC` (-413). CI's `build_signed_app.sh
          # Debug` invocation uses ad-hoc signing — so the helper
          # CANNOT run on this CI runner. We detect that case and
          # skip the runtime check; the self-path logic itself is
          # exercised in `e2e_helper_local.sh` against properly-
          # signed builds + by the macOS Console developers when
          # debugging.
          set -o pipefail
          APP_REL="build_app_output/DerivedData/Build/Products/Debug/CLI Pulse Bar.app"
          HELPER="$(pwd)/$APP_REL/Contents/Helpers/cli_pulse_helper"
          test -x "$HELPER" || (echo "::error::helper not found at $HELPER" >&2; exit 1)

          # Probe with set +e so we capture exit + signal cleanly.
          set +e
          DIRECT_PATH="$("$HELPER" self-path 2>/dev/null)"
          DIRECT_EXIT=$?
          set -e

          if [[ $DIRECT_EXIT -eq 137 || $DIRECT_EXIT -eq 9 ]]; then
            echo "note: helper killed by AMFI on ad-hoc CI signing (exit $DIRECT_EXIT)" >&2
            echo "      This is EXPECTED — restricted entitlements + ad-hoc cert =" >&2
            echo "      AMFI -413. The self-path logic is verified against signed" >&2
            echo "      builds via scripts/e2e_helper_local.sh. Skipping runtime" >&2
            echo "      check at this CI tier." >&2
            echo "OK: ad-hoc CI accepts helper bundle structure; runtime check deferred"
            exit 0
          fi

          if [[ $DIRECT_EXIT -ne 0 ]]; then
            echo "::error::helper exited unexpectedly with $DIRECT_EXIT" >&2
            exit 2
          fi

          LABEL_PATH="$(/bin/sh -c 'exec -a "cli_pulse_helper" "$0" self-path' "$HELPER")"
          CWD_ROOT_PATH="$(cd / && /bin/sh -c 'exec -a "cli_pulse_helper" "$0" self-path' "$HELPER")"

          echo "direct            → $DIRECT_PATH"
          echo "argv0=label       → $LABEL_PATH"
          echo "argv0=label,cwd=/ → $CWD_ROOT_PATH"

          # All three resolutions must agree, and all three must
          # contain Contents/Helpers/cli_pulse_helper.
          if [ "$DIRECT_PATH" != "$LABEL_PATH" ] || [ "$DIRECT_PATH" != "$CWD_ROOT_PATH" ]; then
            echo "::error::self-path resolution differs across argv0 / cwd contexts (Codex P1④ regression)" >&2
            exit 2
          fi
          case "$DIRECT_PATH" in
            */Contents/Helpers/cli_pulse_helper)
              echo "OK: helper resolves to Contents/Helpers/cli_pulse_helper under launchd-style argv0"
              ;;
            *)
              echo "::error::self-path is not Contents/Helpers/cli_pulse_helper: $DIRECT_PATH" >&2
              exit 2
              ;;
          esac

  verify-mas-archive-excludes-launchagent:
    # Mac App Store builds cannot ship the unsandboxed Swift
    # LaunchAgent helper: ASC rejects nested unsandboxed executables
    # with ITMS-90296. The Developer ID/debug path is still covered
    # by `build-signed-app` above; this job pins the MAS path to the
    # opposite contract so a future helper-embedding change cannot
    # accidentally reintroduce the rejected artifact.
    name: Verify MAS archive excludes LaunchAgent helper
    needs: [decide, test-helper-swift]
    runs-on: macos-15
    timeout-minutes: 25
    if: needs.decide.outputs.run_full == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4

      - name: Show Xcode
        run: xcodebuild -version

      - name: xcodebuild archive (ad-hoc signing)
        run: |
          set -o pipefail
          mkdir -p build/ci-archive
          xcodebuild archive \
            -project "CLI Pulse Bar/CLI Pulse Bar.xcodeproj" \
            -scheme "CLI Pulse Bar" \
            -destination "generic/platform=macOS" \
            -archivePath build/ci-archive/CLIPulse-macOS.xcarchive \
            -configuration Release \
            -quiet \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGN_STYLE=Manual \
            DEVELOPMENT_TEAM=""

      - name: Strip Swift LaunchAgent helper from MAS archive
        # v1.20 post-Phase 4D: the `Build Helper Binary (Swift)` build
        # phase now always embeds `Contents/Helpers/cli_pulse_helper`
        # in the raw `xcodebuild archive` output, because the same
        # archive is also reused for DEVID DMG builds where the helper
        # IS shipped. For MAS submission, build-appstore.sh strips
        # helper + plist post-archive (its lines 218-227). CI mirrors
        # that strip here so the verify step below operates on the
        # same artifact ASC actually receives — protecting against
        # the real ITMS-90296 rejection class, not the (now-spurious)
        # "did Build Helper Binary land helper in archive" assertion.
        run: |
          set -o pipefail
          APP="build/ci-archive/CLIPulse-macOS.xcarchive/Products/Applications/CLI Pulse Bar.app"
          if [[ -e "$APP/Contents/Helpers/cli_pulse_helper" ]]; then
            rm -f "$APP/Contents/Helpers/cli_pulse_helper"
            rmdir "$APP/Contents/Helpers" 2>/dev/null || true
            echo "Stripped unsandboxed helper binary from MAS archive"
          fi
          PLIST="$APP/Contents/Library/LaunchAgents/yyh.CLI-Pulse.helper.agent.plist"
          if [[ -e "$PLIST" ]]; then
            rm -f "$PLIST"
            rmdir "$APP/Contents/Library/LaunchAgents" 2>/dev/null || true
            echo "Stripped LaunchAgent plist from MAS archive"
          fi

      - name: Verify Info.plist version substitution (v1.20 A8)
        # Per PLAN_v1.20_2026-05-13 §A8 + Gemini round 1 review CRITICAL:
        # the 5 Info.plist files use $(MARKETING_VERSION) +
        # $(CURRENT_PROJECT_VERSION) substitution instead of hardcoded
        # values. If Xcode build settings ever fail to propagate
        # MARKETING_VERSION to one of the targets, the literal
        # "$(MARKETING_VERSION)" string lands in the shipped Info.plist
        # and ASC ingestion hard-rejects with an invalid version format.
        # CHECK THE COMPILED PLIST (in the built .app), not the source —
        # source legitimately contains the literal var.
        run: |
          set -o pipefail
          ARCHIVE="build/ci-archive/CLIPulse-macOS.xcarchive"
          APP="$ARCHIVE/Products/Applications/CLI Pulse Bar.app"
          INFOS=(
            "$APP/Contents/Info.plist"
            "$APP/Contents/Library/LoginItems/CLIPulseHelper.app/Contents/Info.plist"
          )
          for plist in "${INFOS[@]}"; do
            if [[ ! -f "$plist" ]]; then
              echo "::error::compiled Info.plist missing: $plist" >&2
              exit 1
            fi
            short=$(plutil -extract CFBundleShortVersionString raw -o - "$plist" 2>/dev/null || echo "")
            build=$(plutil -extract CFBundleVersion raw -o - "$plist" 2>/dev/null || echo "")
            if [[ -z "$short" || "$short" == *'$('* ]]; then
              echo "::error::CFBundleShortVersionString unexpanded in $plist (got: '$short')" >&2
              echo "::error::Build settings did not propagate MARKETING_VERSION to this target — ASC will hard-reject" >&2
              exit 1
            fi
            if [[ -z "$build" || "$build" == *'$('* ]]; then
              echo "::error::CFBundleVersion unexpanded in $plist (got: '$build')" >&2
              exit 1
            fi
            echo "  $plist: $short ($build) — OK"
          done

      - name: Verify MAS archive does not contain Swift LaunchAgent helper
        run: |
          set -o pipefail
          ARCHIVE="build/ci-archive/CLIPulse-macOS.xcarchive"
          APP="$ARCHIVE/Products/Applications/CLI Pulse Bar.app"
          PLIST="$APP/Contents/Library/LaunchAgents/yyh.CLI-Pulse.helper.agent.plist"

          echo "=== check 1: no Swift LaunchAgent helper Mach-O ==="
          ! test -e "$APP/Contents/Helpers/cli_pulse_helper" \
            || (echo "::error::MAS archive contains unsandboxed Swift helper after strip" && exit 1)

          echo "=== check 2: no LaunchAgent plist ==="
          ! test -e "$PLIST" \
            || (echo "::error::MAS archive contains LaunchAgent plist after strip" && exit 1)

          echo "=== check 3: LoginItem remains present for MAS runtime ==="
          test -d "$APP/Contents/Library/LoginItems/CLIPulseHelper.app" \
            || (echo "::error::MAS archive missing CLIPulseHelper.app LoginItem" && exit 1)

          # Codex review (PR #39): assert LoginItem is sandboxed. ITMS-90296
          # rejects any unsandboxed nested binary in a MAS archive, not just
          # the Swift LaunchAgent helper. Even though ENABLE_APP_SANDBOX=YES
          # in the Xcode project, this guard catches accidental drift if
          # build settings change or if `CLIPulseHelper.entitlements`
          # source loses its sandbox key.
          echo "=== check 4: LoginItem entitlements include app-sandbox=true ==="
          LOGIN_ITEM_ENT_FILE="CLI Pulse Bar/CLIPulseHelper/CLIPulseHelper.entitlements"
          if [[ ! -f "$LOGIN_ITEM_ENT_FILE" ]]; then
            echo "::error::source LoginItem entitlements file missing at $LOGIN_ITEM_ENT_FILE" >&2; exit 1
          fi
          if ! grep -q "com.apple.security.app-sandbox" "$LOGIN_ITEM_ENT_FILE"; then
            echo "::error::CLIPulseHelper.entitlements missing com.apple.security.app-sandbox key" >&2
            echo "::error::ITMS-90296 will reject the MAS upload — sandbox key must be explicit so verifier can detect drift" >&2
            exit 1
          fi
          if ! awk '
            /<key>com.apple.security.app-sandbox<\/key>/ { found=1; next }
            found && /<true\/>/ { print "ok"; exit }
            found && /<false\/>/ { exit }
          ' "$LOGIN_ITEM_ENT_FILE" | grep -q ok; then
            echo "::error::CLIPulseHelper.entitlements has app-sandbox key but value is not <true/>" >&2
            exit 1
          fi

          echo "OK: MAS archive excludes unsandboxed LaunchAgent helper and keeps the sandboxed LoginItem runtime"
